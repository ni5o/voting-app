apiVersion: apps/v1
kind: Deployment
metadata:
  name: voting-app-result-frontend
  labels:
    app: voting-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: voting-app-result-frontend
  template:
    metadata:
      labels:
        app: voting-app-result-frontend
    spec:
      containers:
      - name: voting-app-result-frontend
        image: ghcr.io/ni5o/voting-app-result-frontend:latest
        ports:
        - containerPort: 3000
        env:
        - name: BACKEND_PROTOCOL
          valueFrom:
            secretKeyRef:
              name: voting-app-secrets
              key: BACKEND_PROTOCOL
        - name: BACKEND_HOST
          valueFrom:
            secretKeyRef:
              name: voting-app-secrets
              key: BACKEND_HOST
        - name: BACKEND_PORT
          valueFrom:
            secretKeyRef:
              name: voting-app-secrets
              key: BACKEND_PORT
---
apiVersion: v1
kind: Service
metadata:
  name: voting-app-result-frontend
  labels:
    app: voting-app
spec:
  selector:
    app: voting-app-result-frontend
  ports:
  - port: 3000
    targetPort: 3000
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: voting-app-result-frontend
  labels:
    app: voting-app
spec:
  to:
    kind: Service
    name: voting-app-result-frontend
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
